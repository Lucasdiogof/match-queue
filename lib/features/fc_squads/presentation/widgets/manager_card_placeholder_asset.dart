/// Silhueta de carta do técnico (mesma linguagem visual de
/// [cardPlaceholderAsset], escudo próprio) usada como fundo do cardzinho de
/// técnico no campo (ver [LineupManagerCorner]) -- lê como "a mesma carta,
/// só de técnico" em vez de um ícone genérico.
const String managerCardPlaceholderAsset =
    'assets/brand/manager_card_placeholder.png';
